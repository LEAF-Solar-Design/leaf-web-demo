export const START_BOARD_COPY = Object.freeze({
  heading: 'Project board',
  returnToDrawing: 'Return to drawing',
  gloss: 'Run uses an existing tool. Build creates a new tool. Review the proposed action before it runs.',
  mockGloss: 'Tool building requires a signed-in conversation.',
})
