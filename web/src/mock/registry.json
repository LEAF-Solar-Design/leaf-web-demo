{
  "tools": [
    {
      "name": "count-by-layer",
      "icon": "layers",
      "version": "1.0.0",
      "description": "Counts entities per layer. The safest read-only tool — a good first run.",
      "kind": "script",
      "engine_op": "count_by_layer",
      "params": { "type": "object", "properties": {}, "required": [] },
      "returns": { "type": "object" },
      "capabilities": ["drawing.read"],
      "provenance": { "author": "agent", "created": "2026-07-17T00:00:00Z" }
    },
    {
      "name": "highlight-panels-near-edge",
      "icon": "bulb",
      "version": "1.0.0",
      "description": "Highlights panels whose centroid is within a given distance of the roof edge.",
      "kind": "script",
      "engine_op": "highlight_near_edge",
      "params": {
        "type": "object",
        "properties": {
          "distance_in": { "type": "number", "title": "Distance (inches)", "default": 60 }
        },
        "required": []
      },
      "returns": { "type": "object" },
      "capabilities": ["drawing.read"],
      "provenance": { "author": "agent", "created": "2026-07-17T00:00:00Z" }
    },
    {
      "name": "measure-panel-area",
      "icon": "dimension",
      "version": "1.0.0",
      "description": "Measures the area of every panel and reports totals; highlights the largest.",
      "kind": "script",
      "engine_op": "measure_area",
      "params": { "type": "object", "properties": {}, "required": [] },
      "returns": { "type": "object" },
      "capabilities": ["drawing.read"],
      "provenance": { "author": "agent", "created": "2026-07-17T00:00:00Z" }
    },
    {
      "name": "delete-marked-panel",
      "icon": "delete",
      "version": "1.0.0",
      "description": "Deletes the marked panel and stores the result as a new, undoable drawing version.",
      "kind": "script",
      "engine_op": "delete_marked_panel",
      "params": {
        "type": "object",
        "properties": {
          "handle": { "type": "string", "title": "Panel handle", "default": "" }
        },
        "required": []
      },
      "returns": { "type": "object" },
      "capabilities": ["drawing.write"],
      "provenance": { "author": "agent", "created": "2026-07-17T00:00:00Z" }
    },
    {
      "name": "select-by-layer",
      "icon": "set-layer",
      "version": "1.0.0",
      "description": "Selects and highlights every entity on a chosen layer.",
      "kind": "script",
      "engine_op": "select_by_layer",
      "params": {
        "type": "object",
        "properties": {
          "layer": { "type": "string", "title": "Layer", "default": "Panels" }
        },
        "required": ["layer"]
      },
      "returns": { "type": "object" },
      "capabilities": ["drawing.read"],
      "provenance": { "author": "agent", "created": "2026-07-17T00:00:00Z" }
    },
    {
      "name": "list-linked-service-items",
      "version": "1.0.0",
      "description": "Placeholder fixture for a tool projected from a connected MCP service (standardization slice 8c); not runnable until upstream tool projection ships.",
      "kind": "script",
      "engine_op": "list_linked_service_items",
      "params": { "type": "object", "properties": {}, "required": [] },
      "returns": { "type": "object" },
      "capabilities": ["drawing.read"],
      "provenance": { "author": "agent", "created": "2026-09-04T00:00:00Z" },
      "mcp_source": { "server_id": "abcdef0123456789abcdef01", "tool": "list-items" }
    }
  ]
}
