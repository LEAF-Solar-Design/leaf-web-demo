{
  "name": "leaf-web-demo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Leaf — build CAD tools with AI, run on Leaf. Web demo lane.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5175",
    "proof:cat": "playwright test e2e/cat-operator-proof.spec.mjs e2e/cat-standards-surface.spec.mjs",
    "check:customization": "node scripts/check_customization.mjs",
    "check:staging-fixes": "node scripts/check_run_intent.mjs && node scripts/check_author_staging.mjs && node scripts/check_mock_catalog.mjs && node scripts/check_fetch_budget.mjs && node scripts/check_checkout_identity.mjs",
    "check:checkout-identity": "node scripts/check_checkout_identity.mjs"
  },
  "dependencies": {
    "@auth0/auth0-spa-js": "^2.23.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
