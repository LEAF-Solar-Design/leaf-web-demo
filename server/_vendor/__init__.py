"""Vendored third-party-style internal libraries (synced, never hand-edited).

Contents come from private internal repos via scripts/sync-mushy-code.py; the
pin manifest next to each surface records the upstream commit and per-file
hashes (`python scripts/sync-mushy-code.py --verify`). Edit upstream, then
re-sync — a hand edit here is drift the verify mode will flag.
"""
