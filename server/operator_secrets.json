{
  "_note": "Operator secret broker registry — METADATA ONLY, never a secret value. Each handle maps to a scope, a NON-PRODUCTION environment, a kind, and a TTL. The credential value is minted on demand by the deployment-registered minter (operator_secret_broker.register_minter) and injected into exactly one adapter call; it never appears here or crosses back to the caller. First brokered non-production secrets per the W0.2 decision-4 ledger.",
  "handles": {
    "github_operator_pr": {
      "scope": "github_pr_open_operator_branch",
      "environment": "staging",
      "kind": "github_pat",
      "ttl_s": 900
    },
    "worker_provider": {
      "scope": "disposable_workspace_provider",
      "environment": "staging",
      "kind": "provider_key",
      "ttl_s": 900
    }
  }
}
