# Security baseline: FastAPI 0.141.1, Starlette 1.6.0, and httpx 0.28.1.
# The pre-drift stack pinned Starlette 0.35.1 because its TestClient used
# httpx's removed app= shortcut. The current TestClient supports httpx 0.28+
# and the operator route-surface test uses Starlette's lifespan API.
fastapi==0.141.1
starlette==1.6.0
uvicorn
pydantic
requests
boto3>=1.34,<2
cryptography>=42
python-multipart  # multipart parsing for POST /api/drawings/upload (§19); was installed-but-undeclared
# tests
jsonschema
pytest
# Pin the compatible current TestClient transport explicitly.
httpx==0.28.1
