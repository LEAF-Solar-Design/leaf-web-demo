## Merge audit — documented-residual exception, resolved via `/askall`

**What this PR delivers (the mandate):** the docs-only reconcile arm of `dispatch-staging-deploys.yml` now converges staging instead of stranding a split, with a **never-backwards selection** guarantee. Rounds 3–4 hardened it: a non-ancestor valid supply set (`behind`/`diverged`) fails closed; a same-head `identical` sibling build is used (docs-only-ness is per-push, not per-commit). The frozen relay-script hash and a traced-case test suite pin all of this.

**Why merged over sol-critic's round-5 RED:** the RED names the convergence-**receipt ordering** race (`wait_for_receipt` accepts a receipt without proving it was published after this relay's own deploy landed). This is **not a #519 regression**:

- `wait_for_receipt` was introduced by `6a1f7a5` (#522) and is already on `main`; #519's diff only re-indents its call site (verified: the only `+/-` lines touching it are whitespace).
- The gap already affects the deployable (`yes`) superseder path on `main`; #519 only extends the same unchanged mechanism to the docs-only (`no`) path.
- Its durable fix is **in-lock validation inside the deploy workflow in the infra repo** (`leaf-automation-aws-terraform`) — out of this repo's scope — the same residual class #508 (`d9ca34e`) and #522 (`6a1f7a5`) were each merged over as documented operator exceptions.

**Resolved via `/askall`** (operator-directed): 3 primaries + 2 fresh verify lanes. Codex's initial claim that the SELECTION had a remaining history-rewrite backwards path was **refuted on verification** by two independent families (Codex sol@high and deepseek-scout) plus the sol-critic round-5 note that selection is "otherwise sound": deploying the rewritten-to ancestor is correct convergence to the current tip, and the only alternatives are strictly worse for operator intent. Claims that the receipt race is real, reachable, and pre-existing all held.

**Rollback:** `git revert` this squash-merge restores the prior skip-and-strand relay. The change only affects future build-triggered relay runs; reverting before any docs-only merge triggers it is clean.

**Follow-up:** the receipt-ordering residual is handed to the infra repo for the durable in-lock-validation fix.

CI: all gates green on the merged tree. Never-backwards selection proven by test; frozen script hash `d1110d4e…` recomputed.
