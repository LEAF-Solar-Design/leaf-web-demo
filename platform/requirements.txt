# Platform (canonical Project/Job entity) — runtime + test deps.
# All already present in the demo's environment; pinned loosely for a fresh venv.
fastapi>=0.110
pydantic>=2.6
psycopg[binary]>=3.1
psycopg-pool>=3.2
# test-only
pytest>=8.0
httpx>=0.27          # required by fastapi/starlette TestClient
